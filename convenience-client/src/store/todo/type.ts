export interface Todo {
  id?: string;
  title: string;
  status?: boolean;
  content: string;
  username: string;
  createAt?: Date;
}

// Title:    todo.Title,
// Status:   false,
// Content:  todo.Content,
// UserName: todo.UserName,
// CreateAt: timeFormat,
